@require '../base/units'
@require '../base/colors'

.hero
    &__container
        box-sizing: border-box
        padding: 3rem $normal_padding
        width: 100%
    
    &__title
        color: $text_color
        font-family: arial
        font-size: 1.5rem
        margin: 0
        text-align: center